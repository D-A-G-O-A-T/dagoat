{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the D.A. G.O.A.T. main window: core APIs plus the store plugin (Season 0 — no sidecar). Wallet keys live in Stronghold snapshots managed entirely Rust-side (no JS stronghold plugin).",
  "windows": ["main"],
  "permissions": ["core:default", "store:default"]
}
